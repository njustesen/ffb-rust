// client-only: Java Swing/AWT rendering component — no Rust UI equivalent.
pub struct Coordinatebasedsteppingstrategy;

impl Coordinatebasedsteppingstrategy {
    pub fn new() -> Self { Self }
}

impl Default for Coordinatebasedsteppingstrategy {
    fn default() -> Self { Self::new() }
}
