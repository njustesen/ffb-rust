//! 1:1 translation of `com.fumbbl.ffb.client.state` (client-side game phase
//! state machines). Only the modules translated so far are declared here; the
//! remaining PascalCase stub files (`ClientState.rs`, `*State.rs`, etc.) in
//! this directory and its `logic/` subtree are placeholders for future
//! batches — see `TRANSLATION_TRACKER.md`.

pub mod logic;
