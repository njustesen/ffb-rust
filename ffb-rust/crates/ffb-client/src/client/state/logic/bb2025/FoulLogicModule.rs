// client-only: Java client state machine — superseded by crate::state_dispatch::mod.
pub struct Foullogicmodule;

impl Foullogicmodule {
    pub fn new() -> Self { Self }
}

impl Default for Foullogicmodule {
    fn default() -> Self { Self::new() }
}
