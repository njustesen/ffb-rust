// client-only: Java Swing/AWT rendering component — no Rust UI equivalent.
pub struct Inducementsmenu;

impl Inducementsmenu {
    pub fn new() -> Self { Self }
}

impl Default for Inducementsmenu {
    fn default() -> Self { Self::new() }
}
