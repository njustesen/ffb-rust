// client-only: Java server command handler — superseded by crate::handlers::mod.
pub struct Clientcommandhandlersetpreventsketching;

impl Clientcommandhandlersetpreventsketching {
    pub fn new() -> Self { Self }
}

impl Default for Clientcommandhandlersetpreventsketching {
    fn default() -> Self { Self::new() }
}
