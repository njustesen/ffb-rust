// client-only: Java server command handler — superseded by crate::handlers::mod.
pub struct Clientcommandhandlersketchsetlabel;

impl Clientcommandhandlersketchsetlabel {
    pub fn new() -> Self { Self }
}

impl Default for Clientcommandhandlersketchsetlabel {
    fn default() -> Self { Self::new() }
}
