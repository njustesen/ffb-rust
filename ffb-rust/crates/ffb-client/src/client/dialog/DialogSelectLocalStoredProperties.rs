// client-only: Java Swing dialog UI — headless decisions handled by network_encoder/mod.rs.
pub struct Dialogselectlocalstoredproperties;

impl Dialogselectlocalstoredproperties {
    pub fn new() -> Self { Self }
}

impl Default for Dialogselectlocalstoredproperties {
    fn default() -> Self { Self::new() }
}
