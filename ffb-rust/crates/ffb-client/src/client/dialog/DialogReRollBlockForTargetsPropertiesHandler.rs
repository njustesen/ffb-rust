// client-only: Java Swing dialog UI — headless decisions handled by network_encoder/mod.rs.
pub struct Dialogrerollblockfortargetspropertieshandler;

impl Dialogrerollblockfortargetspropertieshandler {
    pub fn new() -> Self { Self }
}

impl Default for Dialogrerollblockfortargetspropertieshandler {
    fn default() -> Self { Self::new() }
}
