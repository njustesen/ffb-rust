// client-only: Java Swing dialog UI — headless decisions handled by network_encoder/mod.rs.
pub struct Dialogrerollregenerationmultiplehandler;

impl Dialogrerollregenerationmultiplehandler {
    pub fn new() -> Self { Self }
}

impl Default for Dialogrerollregenerationmultiplehandler {
    fn default() -> Self { Self::new() }
}
