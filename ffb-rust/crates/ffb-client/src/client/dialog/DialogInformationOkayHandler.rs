// client-only: Java Swing dialog UI — headless decisions handled by network_encoder/mod.rs.
pub struct Dialoginformationokayhandler;

impl Dialoginformationokayhandler {
    pub fn new() -> Self { Self }
}

impl Default for Dialoginformationokayhandler {
    fn default() -> Self { Self::new() }
}
