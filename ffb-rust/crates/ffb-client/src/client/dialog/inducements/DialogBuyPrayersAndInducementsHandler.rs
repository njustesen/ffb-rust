// client-only: Java Swing dialog UI — headless decisions handled by network_encoder/mod.rs.
pub struct Dialogbuyprayersandinducementshandler;

impl Dialogbuyprayersandinducementshandler {
    pub fn new() -> Self { Self }
}

impl Default for Dialogbuyprayersandinducementshandler {
    fn default() -> Self { Self::new() }
}
