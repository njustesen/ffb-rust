// client-only: Java Swing dialog UI — headless decisions handled by network_encoder/mod.rs.
pub struct Dialogusemortuaryassistanthandler;

impl Dialogusemortuaryassistanthandler {
    pub fn new() -> Self { Self }
}

impl Default for Dialogusemortuaryassistanthandler {
    fn default() -> Self { Self::new() }
}
