// client-only: Java Swing dialog UI — headless decisions handled by network_encoder/mod.rs.
pub struct Dialogswarmingerrorparameterhandler;

impl Dialogswarmingerrorparameterhandler {
    pub fn new() -> Self { Self }
}

impl Default for Dialogswarmingerrorparameterhandler {
    fn default() -> Self { Self::new() }
}
