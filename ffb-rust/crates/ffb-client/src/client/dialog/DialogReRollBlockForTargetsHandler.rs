// client-only: Java Swing dialog UI — headless decisions handled by network_encoder/mod.rs.
pub struct Dialogrerollblockfortargetshandler;

impl Dialogrerollblockfortargetshandler {
    pub fn new() -> Self { Self }
}

impl Default for Dialogrerollblockfortargetshandler {
    fn default() -> Self { Self::new() }
}
