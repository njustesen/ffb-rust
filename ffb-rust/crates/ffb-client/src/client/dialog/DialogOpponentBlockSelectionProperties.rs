// client-only: Java Swing dialog UI — headless decisions handled by network_encoder/mod.rs.
pub struct Dialogopponentblockselectionproperties;

impl Dialogopponentblockselectionproperties {
    pub fn new() -> Self { Self }
}

impl Default for Dialogopponentblockselectionproperties {
    fn default() -> Self { Self::new() }
}
