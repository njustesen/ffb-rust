// client-only: Java Swing/AWT client component — no Rust UI equivalent.
pub struct Iprogresslistener;

impl Iprogresslistener {
    pub fn new() -> Self { Self }
}

impl Default for Iprogresslistener {
    fn default() -> Self { Self::new() }
}
