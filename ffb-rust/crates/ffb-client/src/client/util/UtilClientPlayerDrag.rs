// client-only: Java AWT/Swing utility — no headless equivalent.
pub struct Utilclientplayerdrag;

impl Utilclientplayerdrag {
    pub fn new() -> Self { Self }
}

impl Default for Utilclientplayerdrag {
    fn default() -> Self { Self::new() }
}
