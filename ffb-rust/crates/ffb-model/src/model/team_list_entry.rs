// TODO: full implementation. Stub placeholder for TRANSLATION_TRACKER.md.
pub struct TeamListEntry;

impl TeamListEntry {
    pub fn new() -> Self { Self }
}

impl Default for TeamListEntry {
    fn default() -> Self { Self::new() }
}
