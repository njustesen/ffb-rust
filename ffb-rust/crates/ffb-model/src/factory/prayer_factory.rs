/// 1:1 translation of com.fumbbl.ffb.factory.PrayerFactory.
/// TODO: full implementation (stub - Phase 4 dependency).
pub struct PrayerFactory;

impl PrayerFactory {
    pub fn new() -> Self { Self }
}

impl Default for PrayerFactory {
    fn default() -> Self { Self::new() }
}
